# Database Connection
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL="postgresql://postgres:password@localhost:5432/airline_pss?schema=public"

# Example production connection (with SSL)
# DATABASE_URL="postgresql://user:password@production-host:5432/airline_pss?schema=public&sslmode=require"

# Example for connection pooling (recommended for production)
# DATABASE_URL="postgresql://user:password@pooler-host:6543/airline_pss?schema=public&pgbouncer=true"

# Database Connection Pool Settings (if using connection pooling)
# DATABASE_POOL_SIZE=10
# DATABASE_POOL_TIMEOUT=30000
# DATABASE_POOL_IDLE_TIMEOUT=10000
